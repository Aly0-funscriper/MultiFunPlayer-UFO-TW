<UserControl x:Class="MultiFunPlayer.Plugin.PluginViewBase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MultiFunPlayer.Plugin"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:plugin="clr-namespace:MultiFunPlayer.Plugin"
             xmlns:ui="clr-namespace:MultiFunPlayer.UI.Controls.Views"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="{x:Type plugin:PluginViewBase}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type plugin:PluginViewBase}">
                        <Expander Style="{StaticResource MaterialDesignToolBarExpander}"
                                  material:ExpanderAssist.HorizontalHeaderPadding="0 0 6 0"
                                  Background="{DynamicResource MaterialDesignToolBarBackground}"
                                  IsExpanded="{Binding DataContext.ContentVisible, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ui:PluginView}}}">
                            <Expander.Header>
                                <StackPanel Height="36" Orientation="Horizontal">
                                    <ContentPresenter Content="{Binding ToolBarContent, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </StackPanel>
                            </Expander.Header>

                            <ContentPresenter Margin="20" Content="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
</UserControl>
