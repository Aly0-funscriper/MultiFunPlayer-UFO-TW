<UserControl x:Class="MultiFunPlayer.UI.Controls.Views.PluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MultiFunPlayer.UI.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:plugin="clr-namespace:MultiFunPlayer.Plugin"
             xmlns:s="https://github.com/canton7/Stylet"
             d:DesignHeight="450"
             d:DesignWidth="800"
             x:ClassModifier="internal"
             mc:Ignorable="d">
    <material:Card Margin="0 0 0 20"
                   material:ShadowAssist.ShadowDepth="Depth1"
                   Visibility="{Binding Items.Count, Converter={StaticResource NotZeroToVisibilityConverter}}">
        <StackPanel>
            <controls:NonReloadingTabControl ItemsSource="{Binding Items}" SelectedItem="{Binding ActiveItem}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel MinWidth="75"
                                    Margin="10 0 10 0"
                                    Orientation="Vertical">
                            <TextBlock HorizontalAlignment="Center" Text="{Binding ViewModel.DisplayName}"/>
                            <TextBlock HorizontalAlignment="Center"
                                       FontSize="10"
                                       FontWeight="Light"
                                       Foreground="{Binding View.StatusForeground}"
                                       Text="{Binding View.StatusContent}"
                                       Visibility="{Binding View.StatusContent, Converter={StaticResource StringIsEmptyVisibilityConverter}}"/>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding View}"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </controls:NonReloadingTabControl>
        </StackPanel>
    </material:Card>
</UserControl>
